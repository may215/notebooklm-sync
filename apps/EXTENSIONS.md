# Extensions Setup

## VS Code Extension
1. Go to `apps/vscode-extension`.
2. Run `npm install`.
3. Press F5 in VS Code to run in Debug Extension Host.

## Chrome Extension
1. Open Chrome and go to `chrome://extensions`.
2. Enable "Developer mode".
3. Click "Load unpacked" and select the `apps/browser-extension` folder.
